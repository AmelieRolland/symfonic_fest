<form method="post" action="{{ path('app_band_register_crud_delete', {'id': band_register.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ band_register.id) }}">
    <button class="btn">Delete</button>
</form>
